CREATE TABLE NKY.MERCHANT_USERS 
(
  id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
  Name VARCHAR(100),
  Email VARCHAR(100),
  Phone VARCHAR(100),
  Account_No VARCHAR(100),
  Password VARCHAR(250),
  CONSTRAINT PK_MERCHANT_USERS PRIMARY KEY(id)
)